---
title: 'Ubuntuの日本国内向けapt mirror設定2026'
emoji: '📦'
type: 'tech'
topics:
  - 'ubuntu'
  - 'apt'
  - 'mirror'
  - 'sed'
published: true
published_at: '2022-11-02 00:23'
---

## 結論

### Ubuntu 24.04以降

```sh
sudo sed -i.bak -E \
  '/^URIs:/ {
    N; /Suites:.*-security/! {
      s@http://(jp\.)?archive\.ubuntu\.com/ubuntu/?@http://ftp.udx.icscoe.jp/Linux/ubuntu/@;
      s@http://(jp\.)?ports\.ubuntu\.com/ubuntu-ports/?@http://ftp.udx.icscoe.jp/Linux/ubuntu-ports/@;
    }
  }' \
  /etc/apt/sources.list.d/ubuntu.sources
```

※ Ubuntu 24.04以降であっても、`/etc/apt/sources.list.d/ubuntu.sources`が存在しないことを示すエラーが発生した場合は以下のコマンドを試す。

### Ubuntu 23.10以前（20.04, 22.04等）

```sh
sudo sed -i.bak -E \
  -e '/-security/!s@http://(jp\.)?archive\.ubuntu\.com/ubuntu/?@http://ftp.udx.icscoe.jp/Linux/ubuntu/@g' \
  -e '/-security/!s@http://(jp\.)?ports\.ubuntu\.com/ubuntu-ports/?@http://ftp.udx.icscoe.jp/Linux/ubuntu-ports/@g' \
  /etc/apt/sources.list
```

## 解説

UbuntuのaptリポジトリのURLは、Ubuntu 24.04以降では`/etc/apt/sources.list.d/ubuntu.sources`、23.10以前では`/etc/apt/sources.list`に書かれている。デフォルトで設定されている`http://archive.ubuntu.com/ubuntu/`を適当なミラーのURLに置き換えれば良い。

ただし、書き換えに当たっては注意すべき点がいくつかある。

- VPSやクラウド環境のUbuntuイメージではURLが多少異なっている場合がある。頭に`jp.`がついていたり、末尾に`/`がついていなかったりする。
- i386とamd64以外のアーキテクチャを使っている場合は、デフォルトで`ports.ubuntu.com`が設定されている。書き換える際もそれに対応したミラーのURLを設定する必要がある。
- セキュリティアップデートが配信されるsecurityポケットのURLは書き換えてはいけない。セキュリティアップデートの適用が遅れる原因になる。詳細は https://jyn.jp/what-is-security-ubuntu-com/ を参照。
  - i386やarm64の場合は`security.ubuntu.com`を書き換えないようにすれば問題ない。それ以外のアーキテクチャではsecurityポケットも`ports.ubuntu.com`から配信されるため、スイートに`-security`が含まれるものを除外する必要がある。

## ミラーサーバーの選択

Ubuntu向けの公開ミラーサーバーの一覧は以下のページに記載されている。

https://launchpad.net/ubuntu/+archivemirrors

この記事ではIPAの産業サイバーセキュリティセンターによって運用されている`ftp.udx.icscoe.jp`を選んだ。私の知る限り日本国内で最速のミラーサーバーで、2022年頃から公開されている。

https://ftp.udx.icscoe.jp/

なお、ミラーサーバーを自動的に選択する`apt-mirror` (`mirror://mirrors.ubuntu.com/mirrors.txt`)も存在するが、不安定なサーバーを引くことがあるのでおすすめできない。
